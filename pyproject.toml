[project]
name = "coldstart"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["matplotlib", "requests"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py313"
